// READ ME

Anne Harris
CS 344 - 400
Block 3 - Program 3
smallsh

The smallsh program will compile with one file name smallsh.c

To compile the smallsh program, type the following into your command line:
gcc -o smallsh smallsh.c

To run the smallsh program (without any grading script), type the following into your command line:
smallsh


